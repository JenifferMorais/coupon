spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/coupondb
    driver-class-name: org.postgresql.Driver
    username: coupon
    password: coupon
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
